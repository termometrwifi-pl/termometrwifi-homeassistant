{
  "config": {
    "step": {
      "user": {
        "title": "TermometrWifi",
        "description": "Enter the API address and the key generated in the app (Settings → Home Assistant).",
        "data": {
          "base_url": "API address",
          "api_key": "API key"
        }
      }
    },
    "error": {
      "cannot_connect": "Cannot connect to the API. Check the address.",
      "invalid_auth": "Invalid or revoked API key."
    },
    "abort": {
      "already_configured": "This API key is already configured."
    }
  }
}
